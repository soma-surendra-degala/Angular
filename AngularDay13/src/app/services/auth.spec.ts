import { TestBed } from '@angular/core/testing';

describe('Firebase Auth placeholder', () => {
  it('should load Auth service via AngularFire if configured', () => {
    expect(true).toBeTrue();
  });
});

